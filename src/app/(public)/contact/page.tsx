import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Amazon | Contact",
  description: "Amazon website Contact side",
};

const Contact = () => {
  return (
    <div style={{ marginTop: "100px" }}>
      <h1>
        Contact
      </h1>
    </div>
  );
};

export default Contact;
